<div class="container">
    <div class="thumbnail">
      <div><%= image_tag(@book.photo_url) %></div>
      <h2>Title: <%= @book.title %></h2>
      <h3>Price: $<%= @book.price %></h3>
      <h2><%= link_to "Edit Price", edit_book_path(id: @book.id), class: "btn btn-primary"%> <%= link_to "Delete Entry", book_path(id: @book.id), method: :delete, data: {confirm: "Delete book?"}, class:"btn btn-danger" %></h2>
    </div>
</div>
